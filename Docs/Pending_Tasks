# Adlaire-Design 未タスク

本ファイルは、Adlaire-Designで未策定または未完了のまま残っているタスクだけを集約する管理ファイルである。

完了済みタスクと履歴は本ファイルに残さない。タスクが完了したら該当行を本ファイルから削除する。

---

## 1. 管理ルール

- 本ファイルには未完了タスクだけを記載する。
- タスクを完了した場合は、該当行を削除する。
- 完了済みタスクの保管場所として使わない。
- 未実装リストには、仕様確定済みで実装だけが未完了の項目だけを記載する。
- 仕様未確定、要否未決定、策定中の項目は未実装リストに含めない。
- タスクを追加、削除、分割、統合する場合は、必要に応じて `Docs/Master_Spec`、`Docs/Document_Index`、`Tools/check/check-adlaire-design.sh` を同期更新する。

---

## 2. 未タスク一覧

| ID | 分類 | 未タスク | 完了条件 | 主な更新対象 |
| --- | --- | --- | --- | --- |
| 現時点で該当なし | - | - | - | - |

---

## 3. 未実装リスト

本章は、仕様確定済みで、実装だけが未完了の項目を管理する。

仕様未確定、要否未決定、策定中の項目は本章に含めない。それらは「未タスク一覧」で管理する。

| ID | 分類 | 未実装 | 完了条件 | 主な更新対象 |
| --- | --- | --- | --- | --- |
| AD-TASK-035 | Icon Set | 公式アイコンセット拡張SVG制作 第11弾。`AD-ICON-441` から `AD-ICON-470` までのSVG実体を制作する。 | `Icons/` に対応SVGが配置され、`Docs/Icon_Set_Catalog` の該当アイコンが `実装済み` になり、検査に通過する。 | `Icons/`、`Docs/Icon_Set_Catalog`、必要に応じて `Samples/design/`、`Tools/check/check-adlaire-design.sh` |
| AD-TASK-036 | Icon Set | 公式アイコンセット拡張SVG制作 第12弾。`AD-ICON-471` から `AD-ICON-500` までのSVG実体を制作する。 | `Icons/` に対応SVGが配置され、`Docs/Icon_Set_Catalog` の該当アイコンが `実装済み` になり、検査に通過する。 | `Icons/`、`Docs/Icon_Set_Catalog`、必要に応じて `Samples/design/`、`Tools/check/check-adlaire-design.sh` |
